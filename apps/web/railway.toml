[build]
builder = "nixpacks"
buildCommand = "cd ../.. && pnpm install && pnpm --filter @overlap/web build"

[deploy]
startCommand = "pnpm --filter @overlap/web start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
