[build]
builder = "nixpacks"
buildCommand = "cd ../.. && pnpm install && pnpm --filter @overlap/worker build"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
